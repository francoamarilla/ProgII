package org.example.domain.model.card;

public enum AbilityType {
    /** Efecto permanente, activo en todo momento mientras el Pokémon esté en juego. */
    PASSIVE,

    /** Efecto activable una vez por turno por el jugador. */
    ACTIVE
}
